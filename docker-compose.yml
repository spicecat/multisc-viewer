name: multisc-viewer

services:
  data:
    build: ./data
    volumes:
      - data:/data
  MultiSCViewerR:
    build: ./MultiSCViewerR
    volumes:
      - data:/data
    depends_on:
      - data
  multisc-viewer:
    build: ./multisc-viewer
    ports:
      - "4173:443"
    volumes:
      - data:/data:ro
    depends_on:
      - data
volumes:
  data:
    driver: local
