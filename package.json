{
  "name": "multisc",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "license": "UNLICENSED",
  "author": "",
  "type": "module",
  "scripts": {
    "format": "prettier --write .",
    "dev:server": "pnpm --filter multisc-server dev",
    "dev:viewer": "pnpm --filter multisc-viewer dev",
    "build": "pnpm --filter server build && pnpm --filter multisc-viewer build"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "eslint": "^9.31.0",
    "globals": "^16.3.0",
    "prettier": "^3.6.2",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0"
  }
}
