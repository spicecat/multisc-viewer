% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/init_db.R
\name{init_db}
\alias{init_db}
\title{Initialize the Database}
\usage{
init_db()
}
\value{
Invisibly returns \code{NULL}. Creates or updates the DuckDB
database file.
}
\description{
Creates and populates a DuckDB database from metadata and data
files in the configured data directory. This function reads dataset
and publication metadata, integrates gene expression data, and
loads everything into DuckDB tables.
}
\details{
The function performs the following steps:
\enumerate{
\item Initializes configuration and paths via \code{init()}
\item Creates a DuckDB connection
\item Resets existing tables
\item Creates \code{datasets} and \code{publications} tables with appropriate schemas
\item Populates tables with metadata from JSON files
and gene data from QS2 files
}
}
\examples{
\dontrun{
  init_db()
}
}
